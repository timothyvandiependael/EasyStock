[phases.setup]
nixPkgs = ["dotnet-sdk"]

[phases.install]
cmds = [
  "ls -R",  # Debug line to show contents
  "dotnet restore EasyStock.API.sln"
]

[phases.build]
cmds = [
  "dotnet publish EasyStock.API/EasyStock.API.sln -c Release -o out"
]

[start]
cmd = "dotnet out/EasyStock.API.dll"